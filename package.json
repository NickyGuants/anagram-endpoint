{
  "name": "anagram",
  "version": "1.0.0",
  "description": "create an api endpoint that returns true if a word is an anagram",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
